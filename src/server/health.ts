// Phase 5a.1 T-04 stub — full GET /health handler ships in the T-04 commit.
import type { FastifyInstance } from 'fastify';

export async function registerHealthRoute(_app: FastifyInstance): Promise<void> {
  // Placeholder registered by boot; T-04 replaces with full handler.
}