/**
 * Social detector types — Phase 2 placeholder.
 *
 * The Phase 1 calibration algorithm is intentionally minimal: a single
 * `isSocial(prompt): boolean` export. This module is reserved for future
 * expansion (e.g., per-pattern metadata, locale-specific catalogs,
 * `matchReason` returns for the audit short-circuit).
 *
 * For now, the file is empty so `tsc --noEmit` is happy with the
 * `index.ts` barrel pattern; downstream code can `import type {} from
 * '../social-detector/types.ts'` when new type slots are needed.
 */

export {};
