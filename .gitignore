# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Env / secrets
.env
.env.local
.env.*.local
.env.*

# OS
.DS_Store
Thumbs.db

# Data + models (per CLAUDE.md — SQLite + ONNX ~1GB)
data/
models/

# Claude Code local-only files
/.claude/settings.local.json
/.claude/settings.json
/CLAUDE.local.md

# Captura_projeto outputs (AI-context bundles — gerados localmente, não versionar)
/repo-raw.md
/repo-map.md
/repo-raw-*.md
/repo-map-*.md

# Captura_projeto (ferramenta externa — repo embedded, customizado pra rodar aqui)
/captura_projeto/

# Inception script artifacts (settings backup + test copies, local-only)
/.claude/settings.json.bak
/.claude/settings.json.test
